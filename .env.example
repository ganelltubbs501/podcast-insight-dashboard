# Frontend
VITE_API_BASE_URL=http://localhost:8080
# Required for authentication and DB access. Create a .env.local with these values when running locally.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Backend (server/.env)
GEMINI_API_KEY=
PORT=8080
